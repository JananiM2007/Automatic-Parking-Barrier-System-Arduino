# Automatic-Parking-Barrier-System-Arduino
An Arduino Uno-based automatic parking barrier system using an IR sensor and SG90 servo motor. The barrier opens automatically when a vehicle is detected and closes after the vehicle passes, demonstrating a simple embedded automation solution.
